# PROGRESS.md — Append-only work log

Format per entry: timestamp (UTC) · task ID · what was done · tests run · result.

---

- **2026-07-31T06:15Z · session-1 start** — Empty repository on branch `claude/equity-research-platform-di3r02`. Environment verified: Docker daemon operational (sandbox required `--iptables=false --bridge=none`; image pulls via proxy and container runs confirmed), Python 3.12 + uv, Node 22. Picking up P0.1 (bootstrap) per directive §11.
- **2026-07-31T06:25Z · P0.1** — Created `DIRECTIVE.md` (committed verbatim directive), `PLAN.md` (full 12-phase + cross-cutting decomposition, gate index, dependency graph), `DECISIONS.md` (D-001…D-009), `BLOCKERS.md` (B1 data vendor/keys, B2 IBKR paper account, B3 golden-set labels, B4 LLM keys — all pre-registered so the human can provision in parallel), `TESTING_LEDGER.md` (schema, empty), `BACKLOG.md`, `README.md`, `.gitignore`. Tests: none applicable (docs only). Result: OK.
- **2026-07-31T06:40Z · P1.3/P1.4 (draft)** — Authored infra artifacts while P1.1/P1.2 scaffolds are built in parallel: `docker/backend.Dockerfile` (uv, non-root, migration entrypoint), `docker/frontend.Dockerfile` (standalone, non-root), `docker-compose.yml` (timescaledb `2.17.2-pg16` — tag existence verified by pull; redis `7.4-alpine`; healthchecked dependency ordering), `.env.example`, `.dockerignore`, `.github/workflows/ci.yml` (backend ruff/mypy-strict/pytest, frontend eslint/tsc/build, security job: tracked-.env check + pip-audit + npm audit), `.pre-commit-config.yaml` + `scripts/check_no_env.sh` + `scripts/check_secrets.sh`. **Verification pending** — these are drafts until P1.5 gate runs them for real; committed now to keep remote state current. Tests: none runnable yet (scaffolds in flight). Result: drafted.
- **2026-07-31T07:05Z · P1.1** — Backend scaffold complete (built by parallel subagent, independently re-verified). Tests: `ruff format --check` (32 files clean), `ruff check` incl. newly enabled pydocstyle D-rules (pass), `mypy --strict backend` (23 files, no issues), `pytest` (16 passed, 89% cov — uncovered lines are live-service checkers exercised at G1), alembic heads/history/offline-SQL + negative connect test. Result: OK, committed `16185bb`.
- **2026-07-31T07:05Z · P1.2** — Frontend scaffold complete (parallel subagent): Next 15.5.22, React 19, Tailwind v4, shadcn/ui stock, TanStack Query; Overview health page polling `/api/health`; 10 future sections rendered inert (no stub content). Tests: `npm run lint`, `tsc --noEmit`, `npm run build` all clean; zero `any`. Result: OK, committed `628393c`.
- **2026-07-31T07:15Z · P1.4 (fix)** — npm-audit triage per D-008: all 12 high advisories transitive with no non-breaking fix (postcss/sharp bundled inside every published next release; brace-expansion via eslint chain). Switched CI to `audit-ci` with committed, justified allowlist (`frontend/audit-ci.jsonc`, D-010). Added I6 test-honesty guard to CI (pytest `--runxfail` + junit zero-skip check) — plan-audit finding. Tests: `audit-ci` passes locally against the real dependency tree; I6 guard verified (16 tests, 0 skipped). Result: OK.
- **2026-07-31T07:15Z · plan-audit** — Independent audit of PLAN.md vs DIRECTIVE.md (subagent): verdict near-complete; 4 gaps + 2 ordering contradictions found and ALL fixed: P11.8 added (cost-model calibration from paper fills — closes G9's deferred clause), CC.7 added (I6 CI guard, backend side done), CC.8 added (I3 connector contract test, lands with P3.1), repo-wide docstring enforcement enabled in P1.1 lint config, CC.2 re-ordered before its first consumer (P3.10 dep added), P8.1 now depends on G5+G6 with explicit P7-non-blocking note. Result: OK.
- **2026-07-31T07:40Z · P1.3 (fixes during gate)** — Two real defects found by running the stack, both fixed and committed: (1) backend image build failed — hatchling needs `README.md` (pyproject `readme` field) present at project-install time; now copied into the image (`1fee0c0`). (2) backend container exited at start — runtime entrypoint invoked `uv run` as the non-root user whose home dir doesn't exist, so uv couldn't create its cache; entrypoint now execs the venv binaries (`/app/.venv/bin/alembic`, `/app/.venv/bin/uvicorn`) directly. Tests: full compose build + up after each fix. Result: OK.
- **2026-07-31T07:45Z · P1.5 — GATE G1 PASSED** — Full stack verified with `docker compose up` from committed files: db (TimescaleDB 2.17.2-pg16) healthy, redis healthy, backend healthy with Alembic `0001` applied against the real DB and timescaledb extension present; `GET /api/health` → 200 `{status:ok, components:{db:{up,latency_ms},redis:{up,latency_ms}}}` with real latencies; `X-Request-ID` round-trip verified; frontend serves (HTTP 200). Negative test: stopping redis → health honestly reports 503/degraded with `redis.up=false`, and recovers to 200 after restart. CI green on branch head `1fee0c0` (run #3: backend, frontend, security jobs all pass; run #1's failure was on the pre-scaffold commit, expected). **Sandbox caveat, recorded for honesty:** this environment's TLS-intercepting proxy + disabled NAT required a *local, uncommitted* compose override for image builds only (host-network builds; base images remapped to locally CA-patched equivalents of the same pinned tags). Committed files are standard and were additionally validated by CI in a normal environment. **Phase 1 complete.**
- **2026-07-31T07:50Z · session-1 summary** — Completed: P0.1, P1.1–P1.5 (Gate G1 passed), plan-audit corrections (P11.8, CC.7 backend, CC.8 planned, CC.2/P8.1 ordering), D-001…D-010 logged, B1–B4 pre-registered. Failed: nothing outstanding — two P1.3 defects found and fixed during the gate. Blocked: B1–B4 await the human (data vendor + keys, IBKR paper account, golden-set labels, LLM keys) — none block the next tasks. Repo state: `docker compose up` works; all tests pass; CI green. **Note:** no PR exists because this branch is the repository's only branch (and therefore its default) — a PR needs a distinct base branch; revisit if a default branch appears. **Next task: P2.1** (bitemporal schema design doc — extended reasoning; then P2.2+). Independent alternates if P2 stalls: CC.0 (crypto foundation), CC.5 (Playwright harness).
- **2026-07-31T08:05Z · P2.1** — Bitemporal store design written to `DECISIONS.md` D-011: three temporal columns (`valid_from`/`valid_to` half-open event-time, `knowledge_time` = market-knowability per source with documented conservative lags; `ingested_at` audit-only and deliberately NOT used for queries so backfills stay honest), append-only corrections/retractions with latest-knowledge-wins reads, `as_of()` scoped-session query layer with a future-timestamp guard and no unversioned read path, three-layer bypass prevention (private sessionmaker, runtime `do_orm_execute` assertion, import contract), hypertables partitioned on `valid_from` with `(entity_id, valid_from, knowledge_time DESC)` indices, DB-level append-only via role grants. Five rejected alternatives documented incl. why full tx-time intervals and system versioning are wrong here. Tests: none (design doc; every choice becomes a test in P2.2–P2.7). Result: OK. **Next task: P2.2** (BitemporalMixin + first fact tables + migration, implementing D-011 verbatim).
- **2026-07-31T15:05Z · P2.2–P2.5 (attempt 1: interrupted)** — First implementation run (parallel subagents) was killed mid-flight by an account usage limit (~22 min in), leaving partial uncommitted work: mixin/models/migration-0002 drafts plus two test files, with `backend/db/engine.py` mid-refactor (suite broken at collection — `create_db_engine` import removed while `test_health.py` still referenced it). Nothing committed (broken trees are never committed). Relaunched after the limit reset with the implementer instructed to treat the draft critically: keep only what conforms to D-011, rewrite the rest, restore full-suite green. Docker daemon (dead after the suspension) restarted; timescale image intact. Result: in progress.
- **2026-07-31T22:00Z · P2.2–P2.5 (attempt 2: landed)** — Bitemporal store implemented against D-011. P2.2 mixin + securities master + price bars + migration 0002; P2.3 `as_of()` with two-level versioned-subquery rewrite (DISTINCT ON latest-wins + retraction masking) — `with_loader_criteria` was evaluated and rejected as insufficient because latest-wins and retraction masking are not row-local predicates; P2.4 three-layer bypass prevention; P2.5 hypertable on `valid_from` + as-of composite index + append-only triggers (0003) and identity-anchor triggers (0004). Tests: ruff + `mypy --strict` (42 files) clean; suite green at that point. Result: OK.
- **2026-07-31T22:30Z · P2.6/P2.7 + adversarial audit** — Independent gate-test author (forbidden from touching implementation code) and a read-only D-011 conformance auditor ran against the landed code. **Property suite passed** (~80k asserted cases, zero I1 violations, boundary equality and immutability-of-the-past covered). **Bypass leg failed, correctly:** four real unversioned read paths were found and three tests were left failing per I6 — (a) SELECTs embedded in `INSERT ... FROM SELECT` / UPDATE subqueries skipped the ORM hook entirely (the exact "materialize features FROM price_bar" shape Phases 4–6 will write), (b) `session.connection()` and the publicly-exported admin engine gave raw Core reads with no banned import needed — falsifying the module's own docstring claim, (c) textual fragments inside ORM selects executed unversioned, (d) the rewrite marker trusted a `_bitemporal_` **name prefix**, so a user subquery of that name was silently exempted. Audit additionally found two write-side hazards invisible to every test: asyncpg silently reinterprets naive datetimes in **host-local time** (hours-scale silent I1 error), and PG `'infinity'` decodes to a *naive* `datetime.max` that raises `TypeError` on any aware comparison. Result: gate correctly blocked.
- **2026-07-31T23:30Z · P2.4 hardening (all findings closed)** — Core-level guard (`before_execute` + `before_cursor_execute`) installed on every engine `backend.db` creates, admitting only executions carrying a module-private token object compared by identity; DML statements now scanned for embedded fact-table reads; textual fragments name-scanned and rejected fail-closed; name-prefix trust replaced with unforgeable token objects; timezone-awareness validated at the mixin before any I/O; `InfinityDateTime` round-trips PG infinity as an aware sentinel; append-only triggers extended to the identity anchor (migration 0004). Test-infrastructure adjustments made honestly rather than by weakening: the per-test TRUNCATE reset and the append-only **trigger** tests now run on the module-private unguarded engine — the triggers are the defense layer *beneath* the app guard, so routing them through a guarded engine would only re-test the guard and leave the triggers unexercised; the import-contract scan gained an explicit two-file allowlist (each justified, plus a staleness test) rather than a blanket `backend/tests` exemption. The schema test was updated to assert through the new TypeDecorator (intent preserved: DDL type must still be TIMESTAMPTZ NOT NULL). Result: OK.
- **2026-07-31T23:45Z · P2.8 — GATE G2 PASSED** — Full suite as CI runs it (`pytest --runxfail`): **102 passed, 0 failed, 0 errors, 0 skipped** in 40s. Gate conditions: (1) Hypothesis as-of property suite asserted **81,121 cases** against real TimescaleDB 2.17.2-pg16 with **zero** `knowledge_time > as_of` leaks — directive floor is 10,000, enforced by a session-scoped accumulator that fails at teardown and publishes the count as a junit property; (2) bypass-impossibility tests pass, exercising every public session-construction path, hand-built sessions over both private sessionmakers, smuggled EXISTS/scalar subqueries, DML-embedded reads, raw Core connections, the admin engine, textual SQL, forged subquery names, and a subprocess run of the ruff import contract against synthetic violating modules. `ruff` (incl. pydocstyle) and `mypy --strict` (42 files) clean. Deviations from D-011 recorded as **D-012** (triggers instead of role grants — with the residual weakness stated: the single owning role can drop its own triggers; two-surface enforcement; fail-closed textual handling; aware-datetime validation). **Phase 2 complete.** Honest open items, tracked not buried: P2.9 (EXPLAIN pushdown check — P2.5 named it, not yet done; performance not correctness) and P2.10 (retraction-row payload hygiene). **Next task: P3.1** (connector framework) — B1 still blocks the vendor connectors, so CC.0/CC.1/CC.3 are the standing alternates.
- **2026-08-01T00:15Z · operator review of Gate G2 — two findings accepted** — (1) **Bypass fix was structural in detection but default-ALLOW in decision.** `collect_references` does traverse generically (`get_children()`) and exempts only by module-private token identity, so the name-prefix fragility is genuinely gone; but `asof.py` returns and passes the statement through when it detects *no* references, and `_guard.py`'s shape-independent backstop (name-scan of the **final compiled SQL**) is explicitly skipped for anything compiled from a clause element. Safety therefore rested on walker completeness, with the one check immune to walker blind spots disabled exactly where it would matter. Being fixed now: positive sanction stamping of vetted executions + universal final-SQL scan, so an undetected shape raises instead of reading. (2) **The 81,121 property cases measure breadth, not depth** — the Hypothesis strategies randomize *data* (securities, valid intervals, knowledge times, retractions, as-of instants); query *shape* is fixed and shallow. Recorded honestly: **G2's evidence is ~one nesting level.** Depth-parameterized suite (recursive CTEs / subqueries / joins against derived tables, depth ≥ 4, asserting the rewrite applies at every level by inspecting the final SQL) in flight; if it exposes rewriter holes, G2 is reopened rather than explained away.
- **2026-08-01T00:15Z · operator decisions recorded** — B1 resolved to **Sharadar via Nasdaq Data Link** (SF1 ARQ/ARY as-reported, SEP, SFP, ACTIONS); EODHD/FMP rejected for serving restated fundamentals; EDGAR direct for filings; borrow from IBKR not a vendor; **WRDS/CRSP-Compustat to be checked first — it changes the decision and is the Phase 3 gate's reference standard regardless**; **PIT claim must be validated on trial data before any annual purchase** (reconstruct a pre-restatement snapshot, confirm original not restated figures). B2: paper account needs a completed live application first — started now, days to approve, on the Phase 11 critical path. New decisions **D-013** (paper fills are a lower bound on slippage, haircut mandatory — calibrating directly to optimistic fills is the easiest way to turn a losing strategy into a winning backtest), **D-014** (golden-set gate derived from measured intra-rater noise floor, not the literal 85%; 60-doc pilot → rubric fix → 20 blind re-labels → floor; one construct across all documents; 15–20h human time), **D-015** (LLM backfill gated on Phase 8 proving incremental IC; pilot 200 names/5yr/2 models; the pilot is a ledger row), **D-016** (P2.9 runs at first real hypertable volume — not now, not after Phase 4; performance gate with design consequences), **D-017** (role separation ships before Phase 11 and before the §6.11 audit log or `TESTING_LEDGER.md` integrity is claimed trustworthy — "immutable" is false while the owning role can drop its own triggers). Repository: **`main` cut at the last known-good commit** (`6a509c8`, CI green) and pushed, so future work is reviewable by PR; it is not yet GitHub's *default* branch — a repo setting only the operator can change.
- **2026-08-01T00:15Z · correction to an earlier session note** — the half-refactored tree from the second interruption was **reconciled, not reverted**; "treated as untrusted draft" was ambiguous wording. Evidence the reconciliation is complete rather than partially applied: the symbol whose half-removal broke collection (`create_db_engine`) appears nowhere in the tree, `backend/db/engine.py` exposes one coherent API with no legacy duplicate, `mypy --strict` passes on 42 files (which would surface any dangling reference), and the full suite plus CI are green.
- **2026-08-01T04:30Z · P2.4 hardening → default-deny (landed, green)** — Guard inverted to **default-deny at the SQL boundary**: `before_cursor_execute` now name-scans the final compiled SQL of *every* execution with no statement-type skip; SQL naming a fact table runs only if explicitly vetted and stamped. Blind-spot proof: with the structural walker stubbed to see nothing, the pre-fix code executed `SELECT price_bar.close_usd FROM price_bar` and — against real TimescaleDB on an `as_of(2024-01-07)` session — **returned a row whose `knowledge_time` was 2024-01-09**, a live I1 lookahead leak through the sanctioned public read path. Post-fix the same probe raises. **Second leak found unprompted and closed:** `ExecutableDDLElement` returned early in both hooks, and `sqlalchemy.schema.DDL` carries arbitrary SQL — so `conn.execute(sa.DDL("SELECT close_usd FROM price_bar"))` on the **public** `create_admin_engine()` was an unguarded arbitrary read path. Implementation note worth keeping: `before_execute(retval=True)` returning `.execution_options(...)` **does not work** on SQLAlchemy 2.0.51 (options are merged before the event dispatches — verified empirically, not assumed), so the Core sanction is a per-connection pre-compilation grant matched by object identity and revoked at every `before_execute`. Deliberately *not* stamped: selects the walker called clean (stamping on a possibly-blind walker's say-so would reopen the hole) and DDL (arbitrary text). One legitimate path added to the sanctioned set, narrowly: plain DML whose target is a fact table with no embedded read — otherwise ingestion `INSERT ... VALUES` would be refused. Also fixed a false docstring claim that TID251 banned importing `_guard` (it bans only `backend.db.engine`). Tests: 112 passed, 0 skipped; ruff + mypy --strict clean.
- **2026-08-01T04:30Z · P2.12 depth suite → GATE G2 REOPENED** — Depth-parameterized suite built (depth drawn first as a first-class dimension; 4 base × 13 wrapper × 2 top shape families, identity-preserving so the oracle is exact **set equality**, not merely "no future knowledge"; measured nesting depth **1–13**). **Most valuable negative result: the structural rewrite is depth-correct** — across 4,122 asserted cases at depths 1–13, zero violations of "every fact-table reference in the final compiled SQL sits inside the versioned `DISTINCT ON` form", and all 140 refused shapes failed closed having sent no fact-table SQL. **But it found a real I1 leak the 87k-case breadth suite structurally could not see:** the as-of value is frozen into a **cached compiled statement**, so re-executing one statement at a second as-of instant reads the store as of the *first* — returning rows whose `knowledge_time` is in their own future. Mechanism pinned offline (equal cache keys carrying 3 binds vs a compiled object carrying 6; `compiled_cache=None` is a clean control). Trigger is not depth: entity-level `select(PriceBar)` + a second fact table + the first referenced ≥2 places — **the exact query shape Phases 4–6 will write**, and it survives every defence because the SQL text is the correct versioned form and only the bind value is wrong. **Gate G2 is REOPENED** (directive §9.10 — a gate is not passed because it was once marked so). P2.11 fixes the root cause and adds a structural backstop verifying every as-of bind actually sent equals the session's as_of, so the class closes rather than the instance. The 11 failing tests stay failing until then (I6). **Commits are held until the suite is green** (§0.4: never commit with failing tests) rather than landing the passing subset while the rewriter is mid-fix — the partial-application risk flagged earlier this session.
- **2026-08-01T05:30Z · P2.11 — stale as-of bind fixed; GATE G2 RE-PASSED** — Root cause pinned to SQLAlchemy internals, not guessed: `Compiler.visit_bindparam` pairs cache-key binds to compiled binds **by `.key` across the clone lineage**, and `BindParameter._clone` regenerates the key of *anonymous* binds — which `sa.literal()` produces. The second adapter pass therefore produced a compiled bind whose key lineage was disjoint from the cache key, so `construct_params` fell back to the value frozen at first compile. Observed pre-fix at `as_of=2016-01-05`: driver received `$1=2016-01-16`, rows returned with `knowledge_time` 2016-01-07 and 2016-01-11. Fix: every versioned subquery shares one explicitly-keyed non-unique bindparam (an explicit key survives `_clone`); compiled cache stays **enabled** — disabling it measured **+1.75 ms (+16.6%) per as-of query**, ~6× the cost of the verification below. Backstops (D-018): post-rewrite invariant (exactly one distinct as-of bind key, carrying the session's value — a regression to anonymous binds is *detected*) and boundary verification against `context.compiled_parameters`, which is recomputed per execution so a cache hit's stale value is visible. New `AsOfBindIntegrityError` on the public surface (registered in the exact-set-equality surface test — a registration, not a relaxation). The backstop regression test is proven non-vacuous by a companion that disables the backstop and asserts the leak reappears, so it cannot pass for the wrong reason if SQLAlchemy changes. Cost measured, not guessed: ~0.3 ms against a 5.96 ms rewrite, below the end-to-end noise floor. **Verified independently by me: 221 passed, 0 failed, 0 errors, 0 skipped; ruff + `mypy --strict` (44 files) clean.** Gate G2 evidence now includes depth: 75,936 breadth cases + **8,944 depth cases to measured nesting depth 14**, 330 shapes rewritten / 140 refused, zero structural violations at any level. **Gate G2 re-passed on evidence that now covers the axis it originally missed.** Next task: **P3.1** (connector framework) with **P3.2 EDGAR as the reference connector**.
- **2026-08-01T05:10Z · P3.1 + CC.8 DONE — recovered after a container reclaim** — The Phase 3 workflow launched at 01:34 died silently at ~02:17 when the container was reclaimed (evidence: agent transcript stopped growing at 02:17:20, journal held a `started` line and no result, and the `claude` process alive at 04:58 had started at 04:57 under `--session-mode resume`). **My earlier status report that it was "still running, probably in its verification pass" was wrong** — I inferred liveness from a quiet filesystem instead of checking for a live process; the operator's flat-token-usage observation was the correct signal. Lesson recorded: for background work, check process liveness, not file mtimes. The agent had however essentially finished P3.1 before dying. Recovered work verified independently from scratch: `ruff` + `mypy --strict` clean (72 source files), **423 passed / 0 failed / 0 errors / 0 skipped** (202 new tests), CC.8 guard clean. Substance spot-checked against spec rather than taken on trust: `run_kind` backfill|live present, future-`knowledge_time` rejection present, supersession helper present, retry documented and tested as transient-only. Committed `44448d0` and pushed immediately — orphaned-but-good work is one reclaim away from being lost. **P3.2 (EDGAR) never started.** Method change for the rest of Phase 3: no more long unattended background workflows in this environment — they have now been killed three times (twice by usage limits, once by a container reclaim). Remaining work runs in the foreground in smaller units, committing after each.
- **2026-08-01T05:35Z · CC.5 DONE (parallel track D)** — Playwright harness + 3 critical-path Overview tests, backend mocked at the network layer. Two of three cover **failure** paths on purpose: degraded served as a real HTTP 503 **with a full body** (the actual D-005 envelope — the only way to prove `fetchHealth` treats 503 as data rather than a transport error), and unreachable asserting the page shows no Database row, no Redis row, no version, since a dashboard rendering "healthy" while the backend is down is precisely the dishonest display this project forbids. Mock pinned to the real `HealthResponse` contract (`satisfies` + `Equals` key-set assertions at every nesting level), proven to actually bite by three negative checks (extra field / added required field / renamed field) that each fail the existing `tsc` step — so contract drift breaks CI with no new job. Suite **mutation-tested rather than assumed non-vacuous**: forcing components to always render `up`, making the error branch render a reassuring badge, and omitting a mock each fail as they should. Playwright pinned to 1.56.1, the release matching preinstalled Chromium build 1194 (resolved by inspecting `browsers.json`), so no browser download and no `executablePath` override. Verified independently: lint, `tsc --noEmit`, `next build`, 3 e2e passing (24s). Committed `9bee1a9` — **only `frontend/` staged**, verified 0 backend files in the commit, since three sibling agents had in-flight work in the tree. CI gains an `e2e` job. **Honest gap the track refused to paper over → new task CC.10:** §8's ≥70% frontend coverage is NOT met and this does not close it — there is no unit-test runner in `frontend/` at all and Playwright emits no coverage number; three e2e tests are not a coverage measurement.
- **2026-08-01T06:55Z · P3.2 + P3.9 + CC.0 + CC.2 + CC.5 DONE (four parallel tracks)** — After three consecutive deaths of long single workflows (two usage limits, one container reclaim), Phase 3 was restructured into **four independent background agents on disjoint file sets**, with every shared file (deps, `config.py`, `.env.example`) committed *ahead* of them so no two could contend. All four completed; each was verified independently and committed in isolation as it landed, so a reclaim could cost at most one track. **Full gate across all four as one tree: 755 passed, 0 failed, 0 errors, 0 skipped**; ruff, `mypy --strict` (92 files), no-fabrication guard clean.
  - **P3.2/P3.9 (EDGAR)** — `knowledge_time` from the SGML `ACCEPTANCE-DATETIME`, US/Eastern→UTC, with the timezone **verified two independent ways** (EST/EDT offsets across six real filings; 17 CFR 232.13's filing-date rule). Deliberately does **not** trust `data.sec.gov`'s `acceptanceDateTime`: for CIK 1750284 it repeats the Eastern wall clock with a spurious `Z` on 12/12 sampled filings while other CIKs are correct UTC — trusting it would have shifted `knowledge_time` by up to five hours for some issuers, silently. Counter-example fixture committed. Also found live: a nonexistent daily index answers **403, not 404** (indistinguishable from a fair-access block), so quarter listings are enumerated rather than dates constructed; and one accession is commonly listed under **many CIKs** (1,809 of 3,394 on 2024-03-11), forcing the logical key to `(accession_number, cik)` — discovered by running against the live service, not from docs.
  - **CC.0** — Fernet at rest (KEK env-only), masked display with no reveal path, structlog redaction placed *after* `dict_tracebacks` so frame locals are scrubbed too.
  - **CC.2** — default-on CSRF, Redis rate limiting failing **closed** (503, not 429), parameterized-query AST check. Reasoning in D-019.
  - **CC.5** — Playwright harness; two of three tests cover *failure* paths, and the suite is mutation-tested rather than assumed non-vacuous. Honest gap logged as CC.10 (frontend coverage still unmet).
- **2026-08-01T07:00Z · two real defects found by cross-track review** — (1) **`TokenBucket.acquire` could spin forever** (already-committed P3.1 code): exact float comparison after refill can land one ulp short, the next delay rounds to ~0, and the loop spins — a hot spin against a real clock, unbounded against an injected one (7.5 GB consumed before the finder killed it). The committed tests missed it because every rate they used had an exactly-representable reciprocal. Fixed with a tolerance (correctness) plus a minimum-sleep floor (liveness). Regression test is **bounded** so a reintroduced defect fails in milliseconds rather than hanging the suite, and was **proven non-vacuous by reverting the fix** — 5 failures across rates 3.0/7.0/0.7/1.1, green again on restore. (2) The rate-limit Redis fixture assumed port 6379 was free on the host-network path; it now picks a free port. CI was unaffected, which is exactly why it went unseen. **Both were found by one track reading another's code — the parallel structure paid for itself.**
- **2026-08-01T07:00Z · new blocker B5** — CORRESP/UPLOAD forms: acceptance can precede dissemination by ~a month (accession `0000950170-24-012183` accepted 2024-02-07, disseminated 2024-03-11), making acceptance-as-`knowledge_time` **anti-conservative** for those forms. Not guessed at in code — the connector *measures* the divergence and the forms stay out of the default selection until a human sets policy.
- **2026-08-01T12:20Z · P8.1 + CC.10 DONE; Wave-1 salvaged after a mass usage-limit hit** — All five Wave-1 agents hit the **account session limit simultaneously** (~08:00Z, reset 09:10Z). **Nothing was lost:** every track had written substantial code to disk, and `mypy --strict` passed on **136 files** (up from 92 — 44 new files survived). Four finishers were relaunched over the drafts (told to treat them as untrusted: keep what is correct, rewrite what is not), plus two new tracks. Recorded so a future session does not repeat the diagnosis: **check process liveness, not file mtimes** — and salvage-then-relaunch beats restarting a track from scratch.
  - **P8.1 (purged K-fold + embargo)** — verified independently and committed `4a82793`. Validates against an **independent brute-force reference implementation** that must agree exactly; asserts `train + test + purged + embargoed == n` so an observation cannot vanish unnoticed; embargo expressed in **time, not observation count** (serial correlation decays in time), with the resolved duration exposed so it is never ambiguous which form applied; states honestly that it knows nothing about exchange calendars; raises with the fold index rather than returning an empty training split. 80 tests.
  - **CC.10 (frontend unit tests + coverage)** — committed `90ef428`. Vitest + jsdom + RTL + V8 coverage, partitioned from Playwright and verified in both directions. **Coverage measured with nothing excluded from the denominator:** statements 85.96%, branches 93.47%, functions 83.33%, lines 84.90% — §8's ≥70% met on all four. Gate deliberately set at 70% rather than today's 85.96%, leaving the ratchet decision to CC.6 instead of inheriting whatever this suite happened to reach; the threshold was confirmed real by forcing it to 95% and observing the failure. **Proven load-bearing by five mutations** of production source (always-render-ok, links instead of inert entries, weakened type guard, removed refetch interval, removed request timeout), each caught, all sources restored byte-identical. CI gains the coverage step and the frontend half of CC.7's I6 skip-guard, verified end-to-end by planting an `it.skip` (Vitest exits 0; the guard exits 1).
- **2026-08-01T13:30Z · P10.2–P10.4, P9.1, P9.3, CC.1 DONE (parallel wave 2)** — Committed `30060f0` (P10), `2e95615` (P9), `e086f41` (CC.1); decisions transcribed as D-020/D-021/D-022 (`0c3d0f2`) since track agents cannot write state files.
  - **P10 (CPCV/DSR/PBO)** — the validation framework, and the gate is *demonstrated* rather than asserted: across **200 seeds of pure noise** PBO means **0.5017** (asserted within ±0.06 of a coin flip, with the seed spread itself asserted so the claim cannot go vacuous), and the best of 200 noise trials — the search really does manufacture ≈1.3 annualized Sharpe — is certified by DSR in **0/200** cases at 95%, while the paired `trials=1` run on identical data certifies **100%**. Injected signal recovered (true 0.20319, CPCV path mean 0.1993 over 40 seeds, strictly below truth as estimation implies). Found two silent defects in its own metrics: `kurtosis` underflowed to a `ZeroDivisionError` *after* the zero-variance guard passed (Hypothesis found it), and `sharpe_ratio` returned a silent `0.0` on variance overflow — a zero Sharpe reads as "no edge" when the truth was "unusable". Mutation-tested six ways including **training on the test rows**, caught by the pure-noise test. Every formula cites its source; the extreme-value benchmark is checked against exact normal order-statistic expectations rather than against itself. 180 tests, 99% coverage.
  - **P9 (covariance + costs)** — refuses below 3 observations instead of flooring the shrinkage intensity, because flooring would report a shrinkage the code *invented* and that number is exactly what tells an operator how much of the risk model is assumption (D-020). Closed a hole the draft missed: at `n_assets == 1` a 2-observation panel is invertible, so nothing raised and the caller silently got an unshrunk estimate reporting `intensity = 0.0`. Also: constant-panel guard never fired (demeaning leaves ~1e-38 residue, not zero), and a short with no holding period silently paid **zero borrow** — an understatement in exactly the direction D-013 forbids. Cost defaults documented by *error direction*. 164 tests, 99% coverage.
  - **CC.1 (audit log)** — honest that it is not immutable while one role can drop its own triggers, with a test that fails the build if the wording is upgraded. Ordering is by `event_id`, never the clock: `recorded_at` is the transaction-start instant, so under concurrency a later event can carry an *earlier* timestamp than the one it supersedes. Keyset cursor rather than offset paging, because offset paging over an append-only table shifts rows between pages — which in an audit trail reads as tampering. 121 tests.
- **2026-08-01T13:30Z · CI was silently dead for 15 commits — repaired (D-022)** — A step name containing `I3:` parsed as a nested YAML mapping, so GitHub rejected the workflow before starting any job and reported *failure with zero jobs*, which in the commit view resembles a build that has not started. **Every "CI green" reported between run #24 and this fix was a workflow that never ran.** The work itself was verified locally each time (full suite + ruff + `mypy --strict` before each commit), so nothing shipped untested — but the CI half of those statements was untrue, and the root cause was committing a workflow edit without validating the YAML. Fixed; a test now parses every workflow and asserts it declares jobs with steps, because this failure produces no signal where people look for one. First real run after the fix: **security, frontend, e2e all pass**; backend cancelled by the concurrency group when the next commit landed.
- **2026-08-02T06:00Z · Phases 4 and 5 built, plus P2.10/P7.7/P11.2/P12.2/CC.3 — five parallel waves, ~1,900 new tests, 190 mutations applied and 190 killed** — Method that held all night: disjoint file ownership per agent, migration numbers assigned up front, agents forbidden from state files and from every git command, each track verified independently and committed **by named path** as it landed. `git add -A` was used once and immediately swept an agent's mid-mutation `transforms.py` (with `ddof=0` live) into a commit — the discipline exists because of that.
  - **P4.1/P4.2 (universe)** — 224 tests, migration 0011. Survivorship-bias defence lives in one comparison: `is_listed_on` asks "was it listed on D", not "is `delisted_on` NULL"; mutating it to the latter fails 8 tests including all four G4 cases, and the fixture reads a 2020 rebalance through a 2026 as-of so the store already records the delisting — the situation every real research read is in. **Every `build_universe` call refuses today**: the market-cap floor is mandatory and no shares-outstanding source exists, so the refusal happens before any read (D-026). Nothing was invented to make it appear to work.
  - **P5.1/P5.2/P5.3/P5.4 (features)** — 137 + 134 + 250 + 140 tests; 16/16, 19/19, 43/43, 27/27 mutations killed. `cross_sectional_zscore` returned **exact zeros on overflow** — "every security is precisely average", finite, plausible, silent, and the one output its own docstring promised never to produce; overflow is now NaN (D-028). Nine factors' expectations are **pre-registered with citations and hashed into the artefact stamp**, so tuning one to a result changes every later report's `config_hash` (D-029). Ranges are *signed intervals*, not magnitude+flag, so a wrong-signed premium cannot satisfy the magnitude test.
  - **P12.2 (drift)** — 252 tests, 17/17. The result worth keeping: recomputing PSI bins per period rather than fixing them at reference time makes PSI **identically 0.0000000000 at a fifty-sigma shift** — a detector that reports perfect health forever. The broken implementation is kept permanently in the suite, asserted to return exactly 0.0 (D-031).
  - **P7.7 (cost governor)** — 101 tests, 17/17. Enforcement is a client **decorator** holding the inner client privately, so no reachable path skips authorization; a `check()` the caller must remember is one refactor from a call site that no longer checks. There is no `check` operation on the ledger at all — `reserve()` is atomic read-compare-write, so a caller cannot forget to hold something (D-032). Prices are an explicitly **empty** mapping and every lookup raises: a cap enforced against a made-up price enforces nothing.
  - **P11.2 (OMS)** — 195 tests, 56/56. Paper-only is structural: **no function in the package takes a parameter named** `venue`/`broker`/`endpoint`/`live`/…, asserted by AST *and* `inspect.signature`, so there is nothing to configure a live endpoint into; and a live fill has no representation, `FillSource` being `SIMULATED` or `PAPER_BROKER` bound by CHECK (D-033).
  - **P2.10 (retraction payload)** — migration 0012, two CHECKs per table. Writing a retraction previously meant inventing a `close_usd` byte-identical to a real observation. The *second* constraint is what makes it a fix rather than a loosening: dropping `NOT NULL` alone would silently admit observations with missing payloads — the same defect pointing the other way, and harder to spot because an absent number reads as a gap rather than a lie. `retract_fact` writes `sqlalchemy.null()`, not Python `None`, because `is_missing` has a `false` server default that an unset attribute would let fill in (D-030).
- **2026-08-02T06:00Z · four defects that only a real database could find, and one flake I introduced** — CI has a Docker daemon; this container does not, so ~50 integration tests across P2.10/P4/P11.2 had **never executed** until pushed. That gap was reported honestly each time rather than rounded to "done", and it paid: P11.2's first real run failed 5 of 25.
  - **Root cause, checked in the driver rather than inferred:** asyncpg maps a plpgsql `RAISE` (`P0001`) to a generic `DBAPIError`, while `unique_violation` and `check_violation` both become `IntegrityError`. Four tests caught `IntegrityError` while a `BEFORE INSERT` trigger — which fires ahead of every CHECK — refused the row with `P0001`. The store now decides retryability on **SQLSTATE, not exception class**, which also removed a latent bug: a CHECK violation is an `IntegrityError` too, so the old code would have called a permanently-invalid row a retryable conflict and looped forever (D-034).
  - **The concurrency race has three refusal paths, not two** — primary key, chain guard, and the state machine refusing in Python *before any insert* when a loser's `load_order` ran after the winner committed. The accounting assertion (`written + refused == writers`) is what exposed the third; without it two writers were silently absorbed into "not written" twice.
  - **A flake I introduced and had to remove.** Having found the third path, I asserted "at least one loser must reach the database" — in the same commit whose docstring says which mechanism catches a writer is a *scheduling accident*. It failed on the next run (0 database / 5 Python, after 3/2 the run before). Removed; the database path is covered deterministically by two non-racing tests instead. Noted because I had spent an earlier round of this same session proving that an intermittently-failing assertion deserves investigation rather than a re-run, and then wrote one.
  - **A test-ordering bug that read as flaky infrastructure.** Exactly one integration test errored with a connection refusal while every other passed. `backend.db.engine` caches one engine per process; the conftest disposed it only on *teardown*, so the session's **first** integration test inherited whatever engine an earlier module built — against the default `localhost:5432`. The trigger was P5.1's own new `test_compute_lag.py`, which calls `as_of()` and sorts before `integration/`. Fixed by disposing on both sides of the fixture, pinned by a structural test that parses the fixture rather than running it, because reproducing it needs Docker *and* a specific collection order.
- **2026-08-02T06:00Z · D-017's deadline was missed, recorded rather than quietly fixed** — "Role separation ships before Phase 11." P11.2 landed, so Phase 11 started, and **CC.9 was still TODO**. The failure mode D-017 named is live: the dashboard states "immutable audit log" to the operator as fact while the owning role can drop the triggers enforcing it, and the Deflated Sharpe's honesty rests on a `TESTING_LEDGER.md` under the same assumption. CC.9 leads the current wave; until it lands neither claim should be shown to an operator without the caveat.
- **2026-08-02T06:00Z · standing state** — CI green on all 8 checks (backend/frontend/e2e/security ×2 runs). ~4,140 tests. `mypy --strict` clean across 336 files. Migrations 0011–0014 have now applied against real Postgres. **Two gates passed of twelve** (G1, G2). **Seven blockers all needing the human**: B1 data vendor, B2 IBKR paper, B3 golden labels, B4 LLM keys/caps, B5 CORRESP policy, B6 UI→Fabel (nine `[UI]` pages; G4 and G5 both name a *rendered* artefact in their binary condition and cannot be signed off without them), B7 short-interest feed (new — B1 is decided and **none of Sharadar's four feeds carries short interest**; no connector task existed). Unverifiable in this container: whether CC.3's MLflow service still lets **G1** pass from a clean clone — G1 is a passed gate and CC.3 changed the thing it tested.

- **2026-08-03T16:15Z · two more gates, and the end of the buildable work** — **G10 and G12 both pass**, taking the count to four of twelve (G1, G2, G10, G12).
  - **G10 is the one the directive singles out** — *"if your framework finds alpha in random data, it is broken and everything downstream is fiction."* Noise over 128 seeds: `t = −0.055`, annualized `−0.0055 ± 0.100`, 45× and 64× headroom. Signal recovery is **proportional, not merely present**: because the estimator's correlation and the return variance both move with the injected strength, predicted step sizes are 2.378× and 1.562× rather than 2× and 2×; measured 2.559× and 1.574×. A framework with a scale error passes a presence test and fails this one. The closed form is re-derived in-test by 400,000-draw Monte Carlo importing nothing from `backend.backtest`. **No source file needed fixing** — the framework modules were mutated for the study and restored byte-identically.
  - **G10's most valuable output was a hole in G10.** Its first version did *not* catch dropped CPCV purging: mutating `_purge_and_embargo` to return the unpurged mask produced **zero gate failures**, because with point-in-time labels purging moves the reported path Sharpes only slightly. An output-only gate accepts a CPCV with its purge deleted, and the leak grows with the label horizon. Fixed with a *structural* assertion. General lesson (D-040): **a validation gate that only inspects outputs cannot detect a validation control being removed, because the control's whole job is to change outputs by a small amount.**
  - **Gross must be tested even though I4 forbids reporting it.** Net of costs, trading noise reliably loses money (`t = −5.08`), so costs *mask* leaks: at leak fraction 0.10 the gross criteria catch the defect (`t = +3.16`) while net does not (`t = +0.70`). The zero-cost path exists as a labelled diagnostic; no gross figure is reported as a result.
  - **G12 was reported FAIL locally and recorded FAIL**, then passed in CI. Clause 1 verified here; clause 2's halt *landing* needed Postgres, and the agent declined to call a gate passed on a test it had not run (§9.10). CI's daemon ran the 8 landing tests green. The gate re-measures its own weakness: power 0.11/0.41/0.78 at 1/2/3σ, ~2.27 years to catch a 1σ decay quarterly.
  - **P11.4 (slicing)** — 88 tests, 7/7 mutations. The lookahead control is that the volume forecast **derives its own bound** (`observed_through = max(session_date)`) rather than accepting a claim, so a caller cannot understate what it consumed. Proven that the cost model will never stop slicing on its own: total cost strictly decreases across n=1..13, impact falls exactly as 1/√n, and commission is *invariant* in n — there is no per-order term — so the bound is exogenous and stated (D-039).
  - **Two more identifier/constraint defects found by real Postgres.** A self-referential FK rendered at 69 bytes against PostgreSQL's 63-byte limit and failed **487 tests** by breaking metadata rendering, not the offending table. A metadata scan added to catch it found **two more** it had masked. Separately, `clearance_fields_iff_cleared` stated its rule over a *conjunction*, which admits any proper subset on the non-clearance branch — an engagement carrying a stray `clears_halt_id` would consume the unique slot and leave a halt **permanently un-clearable while reporting "already cleared"** (D-038).
  - **The per-test TRUNCATE had gone fourteen tables stale**, causing 11 failures. Orders hid it (fresh idempotency key per test); halts could not, being global by design. Now derived from `Base.metadata`.
- **2026-08-03T16:15Z · finalization** — `HANDOVER.md` written: the five human decisions in unblock order, what is built, three things not to misread, and two items unverified for want of a Docker daemon. **Every remaining task is blocked on a human or is a `[UI]` page routed to Fabel.** The single highest-value action is **B1**; four gates, the predictor, and the question the project exists to answer are behind it.
